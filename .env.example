# =============================================================================
# Azure AI Foundry
# =============================================================================
# Project endpoint (required) — found in Azure AI Foundry portal > Project > Overview
AZURE_AI_PROJECT_ENDPOINT=https://<your-project>.services.ai.azure.com/api

# Model deployments — deploy these in your Foundry project
MODEL_DEPLOYMENT_NAME=gpt-4o              # Used by: Researcher, Synthesizer
MODEL_DEPLOYMENT_NAME_FAST=gpt-4o-mini    # Used by: Critic, Planner, Source Workers

# AZURE_CLIENT_ID=
# AZURE_TENANT_ID=
# AZURE_CLIENT_SECRET=
#
# =============================================================================
# Data Sources
# =============================================================================
# GovInfo API key (optional — falls back to DEMO_KEY with rate limits)
# GOVINFO_API_KEY=your-api-key

# =============================================================================
# Tracing (optional)
# =============================================================================
# Connect Application Insights in Foundry portal (Tracing tab) then set:
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...
# AZURE_TRACING_GEN_AI_CONTENT_RECORDING_ENABLED=true
